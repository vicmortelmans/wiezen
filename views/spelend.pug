div.container_spelend 
    #spelSpeler1 
        p #{speler1}
        p #{tricks[speler1]}
    #spelSpeler2
        p #{speler2}
        p #{tricks[speler2]}
    #spelSpeler3
        p #{speler3}
        p #{tricks[speler3]}
    #kaartenSpeler1 
        p #{speler0}
        p #{tricks[speler0]}
    #kaartenSpeler2
        p #{trump}
        p #{players_with_highest_bid}: #{highest_bid}
        p #{player_turn}
    #kaartenSpeler3 
    #eigenKaarten
        each c in cards
            div.card(class = c.color)(class=c.clickable && "clickable", data-card = c.card) #{c.unicode}
    #cards_on_table
        each c in cards_on_table
            div.card(class = c.color) #{c.unicode}
            
script.
    document.querySelectorAll('.clickable').forEach(item => {
        item.addEventListener('click', function () {
            ws.send(this.dataset['card'])
        })
    })

    //@ sourceURL=spelend.js

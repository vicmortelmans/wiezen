div.container_spelend 
    #spelSpeler1 
        p #{speler1}
    #spelSpeler2
        p #{speler2}
    #spelSpeler3
        p #{speler3}
    #kaartenSpeler1 
    #kaartenSpeler2 
    #kaartenSpeler3 
    #tafelKaarten 
    #eigenKaarten
        each c in cards
            div.card(class=c.clickable && "clickable", data-card = c.card) #{c.unicode}
    #cards_on_table
        each c in cards_on_table
            div.card_on_table #{c.unicode}
            
script.
    document.querySelectorAll('.clickable').forEach(item => {
        item.addEventListener('click', function () {
            ws.send(this.dataset['card'])
        })
    })
    //@ sourceURL=starten.js
